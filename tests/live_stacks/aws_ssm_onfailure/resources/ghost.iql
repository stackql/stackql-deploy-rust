/*+ exists, postdelete_retries=2, postdelete_retry_delay=3 */
SELECT 1 AS count

/*+ create */
SELECT 1 AS count

/*+ delete, retries=1, retry_delay=0 */
DELETE FROM aws.s3.buckets
WHERE bucket = '{{ bucket_name }}'
AND region = '{{ region }}'
